const hardhatConfig = {
    defaultNetwork: "rinkeby",
    networks: {
      hardhat: { // Default network, can be removed from here and will still work
      }
    },
    solidity: "0.8.4",
  }

module.exports = {
    hardhatConfig
};